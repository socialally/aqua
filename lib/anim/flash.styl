// vars

// @stylint off
@keyframes flash {
  0%, 50%, 100% {
    opacity: 1;
  }

  25%, 75% {
    opacity: 0;
  }
}
// @stylint on

// blocks
flash-block                 := {
  -webkit-animation-name    : flash
  animation-name            : flash
}

// rules
.flash
  {flash-block}
